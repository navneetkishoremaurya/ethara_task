# SPA only — no docker-compose "backend" host (Railway/nginx crash fix).
# docker-compose sets API_PROXY_TARGET so entrypoint adds /api proxy.
server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
