# Railway: uses Nixpacks (no Dockerfile in this folder).
# docker-compose uses Dockerfile.compose instead.

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start"
restartPolicyType = "on_failure"
